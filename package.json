{
  "name": "ecs-editor",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "games/*",
    "tools/*"
  ],
  "scripts": {
    "build": "nx run-many --target=build --all",
    "test": "nx run-many --target=test --all",
    "lint": "nx run-many --target=lint --all",
    "dev": "nx run-many --target=dev --all --parallel",
    "cli": "node tools/cli/dist/cli.js"
  },
  "devDependencies": {
    "@nx/js": "^19.0.0",
    "@nx/vite": "^19.0.0",
    "@nx/workspace": "^19.0.0",
    "@types/node": "^20.0.0",
    "nx": "^19.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}

