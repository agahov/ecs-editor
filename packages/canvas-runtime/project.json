{
  "name": "canvas-runtime",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/canvas-runtime/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/canvas-runtime"
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite build --watch",
        "cwd": "packages/canvas-runtime"
      }
    }
  },
  "tags": ["type:lib", "scope:runtime"],
  "implicitDependencies": ["ecs-core"]
}

